{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "saga-orchestration-service.serviceAccountName" . }}
  labels:
    {{- include "saga-orchestration-service.labels" . | nindent 4 }}
{{- end }}
